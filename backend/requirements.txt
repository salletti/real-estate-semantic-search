# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database — asyncpg for async runtime, psycopg2-binary for Alembic sync ops
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Migrations
alembic==1.13.1

# Qdrant client
qdrant-client==1.9.1

# Semantic embeddings — torch installed CPU-only via Dockerfile RUN before this
sentence-transformers==3.0.1

# Config / validation
pydantic==2.7.1
pydantic-settings==2.2.1

# HTTP client (used internally by qdrant-client and future integrations)
httpx==0.27.0

# Form parsing — dépendance transitive de FastAPI/Starlette, déclarée explicitement
python-multipart==0.0.27

# LLM — OpenAI-compatible client for Groq (V4 gated intent parser)
openai>=1.35.0
